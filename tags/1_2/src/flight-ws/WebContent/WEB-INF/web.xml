<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE web-app PUBLIC "-//Sun Microsystems, Inc.//DTD Web Application 2.3//EN" "http://java.sun.com/j2ee/dtds/web-app_2_3.dtd">

<web-app>
    <display-name>JAX-WS flight-ws</display-name>
    <description>JAX-WS application: flight-ws</description>

    <!-- JAX-WS listener -->
    <listener>
        <listener-class>com.sun.xml.ws.transport.http.servlet.WSServletContextListener</listener-class>
    </listener>

    <!-- Framework listener - perform initialization on deploy -->
    <listener>
        <listener-class>step.framework.web.ContextListener</listener-class>
    </listener>

    <!-- JAX-WS servlet -->
    <servlet>
        <servlet-name>jax-ws-servlet</servlet-name>
        <display-name>JAX-WS servlet</display-name>
        <description>JAX-WS endpoint</description>
        <servlet-class>com.sun.xml.ws.transport.http.servlet.WSServlet</servlet-class>
        <load-on-startup>1</load-on-startup>
    </servlet>

    <!-- JAX-WS servlet mapping -->
    <servlet-mapping>
        <servlet-name>jax-ws-servlet</servlet-name>
        <url-pattern>/endpoint</url-pattern>
    </servlet-mapping>

    <session-config>
        <session-timeout>60</session-timeout>
    </session-config>

</web-app>
